-- ================================================================
-- CHOPKAR — MIGRATION V4
-- 新增: 商家自定义「顾客会员卡页面背景色」(与卡片主色分开)
-- 在 Supabase SQL Editor 执行一次 (可重复执行)
-- ================================================================

alter table public.merchants add column if not exists bg_color text;

-- 说明: bg_color 为会员卡页面背景色 (例 '#1B1917' 暖深 / '#F7F5F3' 米白)。
-- 留空则页面背景按品牌色亮度自动明暗 (旧行为)。
