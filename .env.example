# Supabase (required)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key
SUPABASE_ANON_KEY=your-anon-key

# Member token signing (optional — falls back to SUPABASE_SERVICE_KEY)
MEMBER_TOKEN_SECRET=your-hmac-secret

# Web Push / VAPID (optional — push disabled if missing)
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
CONTACT_EMAIL=your-email@example.com

# Stripe billing (optional — billing disabled if missing)
STRIPE_SECRET_KEY=your-stripe-secret-key
STRIPE_PRICE_ID=your-stripe-price-id
STRIPE_WEBHOOK_SECRET=your-stripe-webhook-secret

# OTP country-code allowlist (SMS pumping fraud protection)
# Comma-separated dial codes without +. Tighten to 60,65 if you only serve MY/SG.
OTP_ALLOWED_CC=60,65,62,66,63,44,1

# Server
PORT=3000
